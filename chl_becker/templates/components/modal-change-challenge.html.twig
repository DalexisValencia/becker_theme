<div class="fixed z-10 inset-0 overflow-y-auto c-modal hidden" id="modal-change-challenge" aria-labelledby="modal-title" role="dialog" aria-modal="true">
  <div class="flex items-end justify-center pt-4 px-4 pb-20 text-center sm:block sm:p-0 mt-36 lg:mt-0">

    <div class="fixed inset-0 bg-modal-blur" aria-hidden="true"></div>
    <span class="hidden sm:inline-block sm:align-middle sm:h-screen" aria-hidden="true">&#8203;</span>
    <div class="inline-block align-bottom bg-white text-left overflow-hidden shadow-xl transform transition-all sm:my-8 sm:align-middle sm:max-w-lg sm:w-full">
      <div class="bg-modal-change-challenge px-6 pt-3 pb-6">
        
        <div>
          <p class="text-3xl text-white font-bold text-center mt-4">¿Estás seguro de cambiar tu video?</p>
          <p class="text-xs text-center text-white font-third"> Recuerda que tu video anterior será eliminado</p>
        </div>

        <div class="flex flex-col lg:flex-row justify-around mt-14 mb-10 gap-2">

          <a class="flex flex-row items-center justify-between lg:w-5/12 p-4 gap-2 bg-primary  text-white cursor-pointer " data-close="modal-change-challenge">
            Regresar
            <img src="/{{ directory }}/front-src/img/vector.png">
          </a>

           <div class="flex flex-row items-center bg-secondary no-hover lg:w-5/12">
                {{form2}}
                
                <input id="btn-change-challenge" value="Cambiar video" data-user="{{ profile.nikname }}" data-challenge="{{ challenge.name }}" class="flex flex-row items-center justify-around p-4 gap-2 bg-secondary text-white cursor-pointer no-hover">
                   <img class="-ml-9" src="/{{ directory }}/front-src/img/vector.png">
                </input>
  
          </div>

        </div>
        
      </div>
    </div>
  </div>
</div> 